# CLAUDE.md — Handex Handoff Express

Contexto de projeto para o assistente. Leia este arquivo antes de qualquer tarefa.

---

## Quem é o usuário

Augusto Brasil, Designer sênior de UX da Fóton, atuando no projeto da CAIXA Econômica Federal. Trabalha com sistemas de design (DSC — Design System CAIXA), handoff para desenvolvimento e ferramentas internas para o time de design. Não é desenvolvedor de formação — contexto técnico deve ser direto e sem jargão desnecessário.

---

## O que é o Handex

Plugin Figma que automatiza o handoff de design. Permite ao designer:
- Registrar frames e escanear tokens de UI contra o DSC
- Anotar specs técnicas sobre elementos do canvas
- Inserir medidas (padding, gap, width, height) no canvas
- Mapear fluxos de tela
- Gerar uma ficha técnica completa no canvas do Figma

**Versão atual:** v6.29.4  
**Documentação:** `BUSINESS_RULES.md` (regras de negócio) · `CHANGELOG.md` (histórico)

---

## `docs/site/` — repositório central de documentação (leitura obrigatória)

Desde 2026-09, `docs/site/` é o site de documentação interno do Handex — HTML estático autocontido (sem build, sem hospedagem externa, repositório permanece privado, nunca GitHub/GitLab Pages), navegável por `docs/site/index.html`. Reúne arquitetura, regras de negócio, Design System (`design-system.html`), changelog e o histórico de propostas/investigações do projeto — hoje 19 páginas.

**Regra:** toda alteração que impacte documentação (nova decisão de produto, mudança de arquitetura, componente novo/alterado no Design System, regra de negócio nova ou revista) precisa atualizar a página correspondente em `docs/site/` **na mesma entrega** — não depois, não "quando sobrar tempo". Cada `.html` é editado diretamente (não há script gerador no momento desta nota — se um for criado depois, atualizar este parágrafo); os `.md` de origem (`BUSINESS_RULES.md`, `docs/*.md`, etc.) continuam sendo a fonte de edição ativa quando existem, e o `.html` correspondente precisa refleti-los.

Isso é a mesma disciplina já exigida entre `guide.html`/`onboarding.js`/tooltips (ver seção "Instruções do sistema devem ficar sempre casadas" nos agentes de UX/frontend) — aplicada agora também à documentação técnica/institucional. Deixar o site defasado silenciosamente é o mesmo tipo de bug de sincronização já visto neste projeto (specs sumindo por dualidade de fonte, guide desatualizado), só que na camada de documentação em vez de UI.

---

## Arquitetura

```
src/plugin/
├── code.js              ← Backend Figma (FONTE — editar aqui)
├── code.bundle.js       ← Backend compilado (GERADO — não editar)
├── ui.html              ← Frontend completo (GERADO — não editar)
├── build.cjs            ← Assembler do ui.html
├── manifest.json        ← Config do plugin para o Figma
├── modules/             ← Fragmentos JS concatenados no build
│   ├── core.js          ← Estado global, navegação, persistência, accordions
│   ├── messages.js      ← Dispatcher único de window.onmessage
│   ├── handoff.js       ← Coleta de dados + geração da ficha no canvas
│   ├── specifications.js← Scan, render de specs, cards de token
│   ├── audit.js         ← Conformidade DSC na UI
│   └── measurement.js   ← Medidas no canvas
├── views/               ← Fragmentos HTML inseridos no build
│   ├── home.html        ← 6 cards de ferramentas (grid 2×3 flex-1)
│   ├── handoff.html     ← Hub por frame (scan + medidas + specs + conformidade)
│   ├── specifications.html
│   ├── flows.html
│   ├── measurement.html
│   ├── guide.html       ← Onboarding com accordions por ferramenta
│   └── modals.html
└── refs/
    ├── _manifest.json   ← FONTE DE VERDADE das libs DSC
    └── _skeleton.json   ← Bundle gerado (~900KB, embarcado no ui.html)
```

**O frontend não usa módulos ES.** Todos os `modules/*.js` são concatenados em um único `<script>` dentro do `ui.html`. Compartilham escopo global em runtime.

**Comunicação backend ↔ frontend:**
- UI → backend: `parent.postMessage({ pluginMessage: {...} }, '*')`
- Backend → UI: `figma.ui.postMessage({...})`
- Roteador: `window.onmessage` em `messages.js`

---

## Scripts essenciais

```bash
npm run bundle:ui        # Reconstrói ui.html (frontend)
npm run bundle:code      # Recompila code.bundle.js (backend)
npm run export:plugin    # Copia os 3 arquivos para handex-plugin/ (distribuição)
npm run refs:update      # Atualiza skeleton das libs DSC + rebuild completo
```

**Após qualquer alteração:** rodar `bundle:ui` e/ou `bundle:code` antes de testar no Figma.  
**Para distribuir:** `npm run bundle:ui && npm run bundle:code && npm run export:plugin`

---

## Remotos git

```bash
origin  → https://github.com/aab-foton/handex_plugin.git   (GitHub)
gitlab  → https://gitlab.foton.la/caixa-design/design-foton.git  (GitLab — principal)
```

**Sempre fazer push para os dois:**
```bash
git push origin main && git push gitlab main
```

---

## Schema de dados (`handoffData`)

```js
{
  _schemaVersion: 2,       // v1 (wizard antigo) é descartado automaticamente
  step1: { titulo, versao, objetivo, status, equipe[] },
  step2: { briefingEnabled, briefingQuestions[], regras[], anexos[] },
  frames: [{               // hub de frames documentados
    id, figmaId, nome, isNewComponent,
    specs: null,           // resultado do scan
    audit: { checkDone, semDesvios, observacoes, ressalvas[] },
    measurements: [],
    createdSpecs: [],
    excecoes: [],
    specGroupNames: {},
    specGroupVisible: {}
  }],
  createdFlows: [],
  nextFlowNumber: 1
}
```

**Nota sobre `createdSpecs[].id`:** refere-se ao id do nó Figma raiz da spec no canvas — sempre um **GROUP** (`figma.group()` agrupando `Conector`/`DotInicio`/`DotFim`/`specCard`), com o `contour` ("Destaque") solto fora do grupo, travado, vinculado por pluginData (`handexSpecMarkerFor`/`handexSpecMarkerId`). Uma migração para FRAME único foi tentada e revertida (ver "Bugs corrigidos relevantes") — não retomar sem alinhamento.

**Nota sobre `specGroupNames`/`specGroupVisible`:** nome legado — refere-se ao agrupamento lógico de specs por letra/categoria na UI, não ao tipo de nó GROUP do Figma.

**Feature oculta — `_aiContext` (2026-08, `modules/design-data.js`):** `exportHandoffData()` e `exportProgress()` injetam um campo `_aiContext` (calculado por `_buildAiContext()`) no JSON exportado, agregando briefing (`step1`/`step2.briefingQuestions`/`regras`), tokens usados nas specs, cenários de exceção, medidas, fluxos e (v6.20.8) padrões de categorização do projeto atual num formato compacto. Objetivo: material de apoio para colar como contexto/prompt em ferramentas externas de geração (ex: Figma Make) ao propor uma tela nova dentro do MESMO projeto — nunca integração automática (não existe canal de API para isso) nem geração feita pelo próprio Handex. Sem botão/UI visível de propósito — funciona em background, sempre presente no export existente. `_aiContext` é dado **derivado**, calculado no momento do export: nunca deve ser persistido em `handoffData` nem sobreviver a um ciclo de import (`importHandoffData()` remove o campo antes do `Object.assign`, de propósito). Ver `docs/figma-api-roadmap-2026.md` (item 4b) para o raciocínio completo de escopo — por que isso fica contido a um projeto e não deve crescer para conhecimento institucional agregado entre projetos.

**`_aiContext.padroesCategorizacao` (v6.20.8, 2026-09-17):** registro (nunca inferência) de qual categoria de spec (Informação Extra/Comportamento/Regra de Negócio/Dados da API) o designer escolheu em cada frame que contém componentes reais do DSC, junto com os componentes (nome + biblioteca de origem, ver abaixo) presentes naquele frame — `[{ componentesDoFrame: [{nome, biblioteca, componentKey}], categoria, categoriaLabel }]`, um item por spec com categoria definida num frame com pelo menos 1 componente vinculado ao DSC. Motivado por questionamento do usuário sobre sugerir categoria automaticamente por ML: investigação (agente `data-design`, 2026-09-17) confirmou que o skeleton das 6 libs DSC não carrega nenhum metadado de FUNÇÃO de componente (só `componentKey` como hash de identidade + tokens de estilo) — não há como inferir "isso é form field vs. feedback vs. navegação" sem heurística de nome de camada, que já é regra proibida neste projeto (ver "Padrão a vigiar" nas correções de conformidade DSC abaixo). Decisão: o Handex nunca infere nem sugere categoria sozinho — só registra fielmente o histórico de decisões já tomadas NESTE projeto, pra uma ferramenta externa (Figma Make) fazer a inferência com mais contexto do que o Handex teria isoladamente. Pré-requisito real pra sugestão automática dentro do próprio Handex, se um dia for retomada: a lib do DSC passar a carregar uma taxonomia funcional por `componentKey` na fonte (ex: pluginData custom no Figma, análogo ao que o hac cogita com `dsc.a11y`) — sem isso, é dado que não existe.

**Botão "Copiar contexto pro Figma Make" (v6.21.0, 2026-09-17, `handoff-summary.html`/`design-data.js`):** o `_aiContext` era feature 100% oculta, sem nenhum acesso via UI — só ia junto no JSON completo exportado. Adicionado botão na tela de Resumo (seção "Exportar") que formata o `_aiContext` como texto de prompt legível (`_formatAiContextAsPrompt`, não JSON cru) e copia pra área de transferência via `navigator.clipboard.writeText` (mesmo padrão já usado em `copyPendingComments`, `handoff.js`) — v6.23.1 adicionou fallback `document.execCommand('copy')` porque o iframe da UI do plugin nem sempre concede a Permissions Policy da API moderna (ver "Bugs corrigidos relevantes"). **Não é integração automática** — reafirmado explicitamente porque foi a pergunta direta do usuário que motivou a feature: não existe canal de API do Figma Plugin nem do Figma Make pra "enviar" o contexto automaticamente; o botão só prepara o texto, colar continua manual. `telasDocumentadas` foi expandido com `componentesDSC` (nome + biblioteca de origem, via `matchedIn` já calculado pelo `audit.js` contra o skeleton — nunca nome de camada isolado) por tela, para dar ao Figma Make a maior chance de reconhecer/reutilizar componentes reais do DSC em vez de recriar do zero — **sem garantia**: depende de o Figma Make ter acesso àquela biblioteca publicada no mesmo arquivo/organização.

**Botão "Baixar imagens dos frames" (v6.24.0, 2026-09-18):** usuário perguntou se o contexto pra IA não deveria levar também a imagem do frame. Investigação (`ClipboardItem` com múltiplos MIME types) mostrou que os tipos de um mesmo `ClipboardItem` representam a MESMA informação em formatos alternativos (ex: texto simples + HTML do mesmo texto), não duas coisas distintas — colar imagem+texto juntos num só Ctrl+V não é garantido: o app de destino (Figma Make) escolheria só um dos dois tipos, resultado incerto. Implementado em vez disso: handler `export-frame-snapshots-for-ai` (`code.js`) gera um PNG por frame documentado (mesma marcação de "Documentação Visual" — só selos/contornos de spec ou marcações de medida, reaproveitando `_hdSnapshotFrameWithNodes`/`_hdCollectSpecContourIds`), devolvido em base64 (`figma.base64Encode`); o frontend baixa cada um como arquivo `.png` separado (`_downloadFrameSnapshotImages`, `design-data.js`) — dois passos (copiar texto + baixar imagens), mas garantido de funcionar, ao contrário da tentativa combinada.

**Bloco "Exportar" virou accordion + hint expandido (v6.29.2/v6.29.3, 2026-09-18):** o checklist inteiro (hint + 5 itens + botão "Exportar selecionados") ficava sempre expandido na tela de Resumo, ocupando espaço constante. Virou accordion (`toggleExportAccordion`, `core.js`) — **aberto por padrão** (decisão explícita do usuário, diferente do Frame Selector de Specs/Medidas que fica recolhido). O hint didático (antes só explicava MD vs JSON em texto corrido) foi expandido pra uma lista com bullet, uma linha por opção (Ficha PDF/Briefing/MD/JSON/Contexto pro Figma Make), e ganhou botão de dispensar (`export-hint`, usando `_dismissContentHint`/`_contentHintDismissed` já existentes em `core.js` — persistido em localStorage, fica fechado entre sessões).
**Checklist de exportação (v6.29.0, 2026-09-18):** os 5 botões individuais de export na tela de Resumo (Briefing/Markdown/JSON/Ficha PDF/Contexto IA) viraram uma lista com checkbox + botão único "Exportar selecionados" — a lista de botões estava crescendo a cada exportação nova adicionada e ficou longa demais. `executeSelectedExports()` (`design-data.js`) executa as marcadas em **sequência** (nunca em paralelo — duas exportações concorrentes colidiriam nos mesmos elementos de UI/estado global), com `handoff-loading-overlay` visível durante toda a fila. As 3 exportações síncronas (Briefing/Markdown/JSON) resolvem na hora; PDF da Ficha e Contexto+Imagens (que abre modal no meio) foram adaptadas para retornar uma `Promise` resolvida só quando a resposta do backend ou a decisão do designer no modal chega — sem isso, a fila avançaria antes da exportação assíncrona terminar de verdade. "Ficha em PDF" é o primeiro item da lista e vem pré-marcado por ser a exportação mais comum do fluxo.
**Botão "Baixar Ficha em PDF" (v6.28.0, 2026-09-18):** exportação REAL do frame da Ficha já gerado no canvas — fidelidade visual total (cores, cards, snapshots), usando `format: 'PDF'` do `exportAsync` nativo da Plugin API (`node.exportAsync({ format: 'PDF' })`), não uma reconstrução via lib externa. Diferente do PDF já embutido no `.zip` completo (`exportHandoff`, que converte o Markdown pra texto corrido via jsPDF) — este é a Ficha de verdade, pixel a pixel. Handler `export-ficha-pdf` (`code.js`) reaproveita `_hdFindExistingFicha` (mesmo critério de busca já usado em `pull-ficha-version-from-canvas`/`insert-ficha-section`) para localizar a Ficha do projeto atual; se ela ainda não existe no canvas, retorna erro tratado (`_handleFichaPdfExported`, `design-data.js`) pedindo pra gerar a Ficha primeiro. Botão na tela de Resumo, junto dos outros exports.
**Modal "Incluir mais frames no contexto?" (v6.27.0, 2026-09-18):** o designer normalmente documenta só PARTES de uma tela (um header, um menu específico) — a tela inteira onde essas partes vivem nunca tinha imagem própria no pacote, já que o snapshot só existe pra frames com spec/medida vinculada. Antes de gerar o pacote, `downloadAiContextPackage()` agora dispara `list-canvas-frames-for-ai` (backend lista `FRAME`/`COMPONENT`/`COMPONENT_SET` de nível superior da página atual, excluindo as Sections do próprio Handex via `handexCategory`) e abre `ai-context-extra-frames-modal` com um checkbox por frame não documentado. Telas já documentadas continuam sempre entrando no pacote, sem precisar marcar nada — o modal é só pra frames ADICIONAIS (telas completas, sem nenhuma marcação de spec/medida, snapshot é a tela inteira via `exportAsync` simples). Seleção vira `extraFrameIds` no `export-frame-snapshots-for-ai`, que agora aceita esse parâmetro opcional além dos frames documentados de sempre.
**Botão único "Baixar contexto + imagens pro Figma Make" (v6.26.0, 2026-09-18):** os 2 botões separados (copiar texto pra clipboard + baixar PNGs individuais) viraram 1 pacote único a pedido do usuário. `downloadAiContextPackage()` (`design-data.js`) dispara `export-frame-snapshots-for-ai` como antes; a resposta monta um `.zip` via JSZip (mesma lib já usada em `exportHandoff`, `handoff.js`, CDN sob demanda) com `contexto-ia.md` (o texto do `_aiContext`) + pasta `imagens/` com um PNG por frame documentado. Fluxo deixou de copiar pra área de transferência — agora é sempre um arquivo baixado, aberto/copiado manualmente de dentro do zip. `copyAiContextForFigmaMake`/`downloadFrameSnapshotsForAi`/`_legacyCopyToClipboard` removidos (substituídos por `downloadAiContextPackage`/`_buildAiContextPackage`).

**`_aiContext.telasDocumentadas[].itensEscaneados` (v6.25.0, 2026-09-18):** usuário testou o contexto real gerado no Figma Make e reportou que era genérico demais pra reconstruir uma tela com fidelidade ("não referencia as libs, não traz o detalhe de cada card... o ideal seria levar tudo, inclusive a nível de código"). Avaliada e descartada a ideia de extrair uma árvore posicional 1:1 de cada frame (x/y/w/h/hierarquia de TODO nó, ao estilo Figma REST API/html.to.design) — investigação (2026-09-18) estimou 200-800+ nós por tela real de produto, gerando 250KB-3MB de JSON por frame se serializado nesse padrão: inviável de colar num prompt e arriscado de rodar (loop de `exportAsync`/leitura por nó pode travar a thread principal do Figma). Em vez disso, reaproveitado 100% o dado que o **scan de auditoria já produz e já está salvo** em `frame.specs[categoria][]` — nome, categoria, `isMarkedCustom` e as propriedades reais já capturadas (`item.properties`: cor exata, espaçamento exato, tipografia exata, não só "tem token ou não") — sem nenhuma extração nova, sem risco de performance (dezenas de KB no máximo, a mesma ordem de grandeza do scan que já roda hoje). Combinado com o snapshot (botão acima), a dupla dá à IA externa referência visual (a imagem) + especificação técnica exata de cada peça que aparece nela (o texto) — o par que uma IA generativa precisa pra replicar com fidelidade, sem os riscos da árvore completa.
**Bug corrigido na mesma leva:** `elementMatchedIn` (usado por `_dscComponentsOfFrame` pra filtrar `componentesDSC`) nunca era preenchido quando o vínculo de um componente vinha só da convenção `[dsc]` no nome (sem `componentKey` reconhecido no skeleton — comum em sub-componentes estruturais ou libs recém-publicadas) — `code.js` só setava `dsElement = true` nesse fallback, nunca `elementMatchedIn`, então esses itens (genuinamente conformes segundo a própria auditoria do plugin) eram descartados silenciosamente do `_aiContext`. Corrigido preenchendo `elementMatchedIn = 'DSC (convenção de nome)'` nesse caso.

**Feature oculta e DESLIGADA — handoff pra plugins de handoff especializado (2026-08-26, `code.js`):** `_writeDscHandoffSummary(node, frame)` grava, via `setSharedPluginData('dsc-handoff', 'frame-summary', ...)` no próprio nó do frame, a lista de `componentKey`/`name`/`nodeType` que o scan já identificou naquele frame — pensado para plugins irmãos de handoff especializado (ex: **hac**, foco em acessibilidade) lerem o mesmo frame depois e pré-preencherem o que puderem, sem o designer repetir seleção/identificação de componente. Controlado por uma única flag, `DSC_HANDOFF_SUMMARY_ENABLED` (topo de `code.js`, perto de `PLUGIN_VERSION`) — **hoje `false`, deliberadamente**: a função já está implementada e testada (lint/bundle limpos), mas o consumo do lado do hac ainda não está consolidado o suficiente para ativar. Quando o hac estiver pronto para consumir, ativar trocando essa flag para `true` — nenhuma outra mudança de código é necessária deste lado. Canal isolado do `_aiContext` acima: namespace/key diferentes (`dsc-handoff`/`frame-summary` vs. `handex`/`context`), propósito diferente (fato bruto de componentes pra outro PLUGIN consumir, não contexto de prompt pra IA generativa), gatilho diferente (a cada save, não só no export). Deliberadamente **não resolve** lib de origem DSC nem categoria de a11y do lado do Handex — essa lógica já existe e é mantida no hac (`_getDscComponentKeyToFrameMap`/`_resolveDscComponentA11yMatch`); duplicar aqui criaria duas cópias divergentes da mesma resolução.

---

## Decisões de produto tomadas (não reverter sem alinhamento)

| Decisão | Razão |
|---|---|
| Dots de conformidade por propriedade **removidos** da ficha | Criavam falsa impressão de 100% conforme; acurácia é responsabilidade do designer |
| **Vetores** filtrados do scan | Shapes primitivos não representam conformidade DS |
| **Frames com filhos DS** filtrados do scan | São contêineres de layout; conformidade vive nos filhos |
| Frames sem nenhum filho DS **mantidos** no scan | Indicam tela 100% custom fora do DS — informação relevante |
| `isDS` por propriedade **não influencia** os toggles de auditoria | São camadas distintas: scan automatizado vs. declaração humana |
| Accordions do botão `⇅` incluem **cards de frame** | Antes só recolhia accordions internos |
| Spec permanece **GROUP + nó solto (`contour`) vinculado por pluginData** — não migrar para FRAME único | Testado em 2026-08: FRAME único exigia duplo-clique para mover Conector/card sem arrastar o marcador junto; não ficou bom na prática. Revertido para o esquema original (clique simples move só o grupo móvel, marcador nunca acompanha) |
| Inserção incremental por subseção **volta a existir** (v6.13.0, 2026-09-15), via handler único `insert-ficha-section` | Tinha sido removida antes (`insert-frame-in-ficha`/`insert-flows-in-ficha`) por duplicar lógica de idempotência entre handlers quase-idênticos; a reintrodução resolve isso reaproveitando as mesmas funções `_hdRebuild*Section` também usadas por `create-handoff`, sem duplicar a montagem |
| Snapshots visuais na Ficha (Snapshot com Specs / Snapshot com Medidas) usam **PNG estático** (`exportAsync`), não réplica de camadas | Avaliação 2026-09-15 comparando com o plugin irmão hac (que usa clone real + snapshot PNG): o Handex já desenha specs/medidas direto sobre o frame original (não precisa de um "canvas de rascunho" separado como o hac), então só o snapshot isolado (sem a réplica de trabalho clonada) resolve o problema real — dar contexto visual na Ficha sem tocar a arquitetura de specs existente |
| Card 3 "User Interface" da Ficha mostra **só itens declarados manualmente como "Componente Personalizado"** (`item.isMarkedCustom`) — não mais qualquer item escaneado com vínculo DSC | Questionamento 2026-09-16: dev não precisa de lista de propriedades de componentes prontos da lib (a lib já documenta); "Necessita revisão" é trabalho pendente do designer, não informação de construção pro dev. O que o dev precisa é só o que vai precisar CONSTRUIR — decisão manual, porque o dado calculado pelo scan (`isCustomComponent`) mede vínculo técnico comprovado, não equivalência estrutural (ex: um dropdown horizontal pode ter algum vínculo fraco e ainda ser estruturalmente diferente do padrão DSC) |
| Seções agregadas "Especificações" e "Medidas" da Ficha (texto puro, listando todos os frames juntos) **removidas**, substituídas por "Documentação Visual" — 1 bloco por frame documentado, com snapshot amplo (frame real + só selos/contornos de spec ou marcações de medida, nunca Conector/specCard) + card de detalhe em texto ao lado | Decisão 2026-09-17: dev não precisa de uma lista de texto desconectada da tela real — precisa ver a marcação sobre a tela (onde cada spec/medida está de fato) e o detalhe ao lado, agrupado por frame. Mais fiel ao modelo do plugin irmão hac (cada área/tela documentada tem seu próprio destaque visual) |
| Botão "Finalizar Registros" **removido** das 4 telas (Tokens/Specs/Medidas/Fluxos) — sobra só "Inserir [X] na Ficha", que vira "Atualizar [X] na Ficha" (ícone `refresh-cw`) assim que aquela seção já foi inserida uma vez (`handoffData._fichaSections[key]`) | Decisão 2026-09-17: `finalizeSection` só chamava `saveAndGoHome` com um toast — nunca sincronizava nada no canvas, e "salvar os dados" já é coberto por `insertSectionInFicha` (chama `collectHandoffData()` antes de enviar e `saveToStorage()` no retorno de sucesso). Dois botões pra uma função e meia era redundância sem ganho — um único botão com estado dinâmico expressa a mesma coisa com menos ruído visual |
| Posição da Ficha **nunca mais é adivinhada por heurística** — é sempre decisão explícita do designer, confirmada uma única vez por projeto (`handoffData._fichaBasePosition`) | Decisão definitiva 2026-09-17, depois de 3 tentativas de heurística automática (ancorar no frame, depois "abaixo" em vez de "à direita", depois corrigir bug de prioridade) todas falharem na prática — ficha continuava nascendo "a milhões de pixels de distância" em cenários reais. Na 1ª geração do projeto, a Ficha já nasce visível/selecionada numa posição sugerida simples (abaixo do frame documentado, ou da seleção, ou do viewport); o designer arrasta ela mesma no canvas do Figma (livre por trás do modal, que cobre só a UI do plugin) e clica "Confirmar posição da Ficha" (`ficha-position-confirm-modal`, sem X/fechar de propósito — fica aberto até confirmar, pra dar fluidez à jornada em vez de banner fixo competindo com o resto da tela). A partir daí, essa posição é a base do projeto pra sempre: "Atualização" substitui exatamente ali, "Nova Versão" nasce ao lado — nunca mais pergunta, nunca mais recalcula |
| Seção "Frames Documentados" da Ficha renomeada para **"Frames Escaneados"** e filtrada — só lista frames com pelo menos 1 item do scan marcado como `isMarkedCustom` (`_hdFrameHasCustomItem`, `code.js`) | Decisão 2026-09-17: mesmo critério já usado no Card 3 "User Interface" (`createSpecList`) — um frame 100% conforme ao DSC não precisa aparecer na Ficha, o dev já usa o componente pronto da lib. Antes a seção listava TODO frame registrado, sem esse filtro — inconsistente com a decisão já tomada sobre o que entra na Ficha (ver entrada "Card 3 'User Interface'" acima) |
| Frame com **"Novo Componente" marcado** (`isNewComponent`) entra na "Frames Escaneados" da Ficha **sempre**, obrigatório, mesmo sem nenhum item `isMarkedCustom` dentro dele (`_hdFrameIsRelevantForFicha`, `code.js`) | Decisão 2026-09-17: o toggle "Novo Componente" já É a declaração de "isso é novo pro DSC" no nível do frame — não faz sentido exigir que o designer também marque itens individuais como personalizados pra esse frame aparecer |
| Quando **nenhum frame** é relevante (sem Novo Componente, sem item `isMarkedCustom`), o plugin **pergunta** antes de inserir/gerar a Ficha (modal `frames-relevance-modal`) se o designer quer incluir a lista completa de frames mesmo assim — nunca omite nem inclui tudo silenciosamente | Decisão 2026-09-17: scan sem nada fora do DSC pode ser real (frame 100% conforme) ou pode ser esquecimento do designer em marcar algo — decisão explícita evita os dois erros silenciosos. Fluxo técnico: `check-frames-relevance` (backend, sem escrita no canvas) → se `hasRelevantFrame` for false e houver frames, abre o modal → resposta vira `includeAllFrames` no `insert-ficha-section`/`create-handoff` real |
| Ficha tem **largura fixa em cascata** (não mais Hug): `mainContainer` 1080px → `fichaTecnica` 952px (1080 − padding 64×2) → `content` 904px (952 − padding 24×2) → `block` de cada frame 872px de área útil (904 − padding 16×2). Altura sempre Hug em todos os níveis | Decisão de produto 2026-09-17: usuário achou o layout vertical (empilhado, largura única de card) melhor que a proposta anterior de colunas lado a lado — fica bom pra exportar em PDF e se adequar a uma visão de protótipo navegável. Largura fixa garante consistência visual entre fichas de projetos diferentes |
| Snapshot de "Documentação Visual" **nunca corta** — largura sempre a proporção real do frame documentado, limitada a 872px (a área útil do bloco); se o frame for mais largo, a imagem inteira reduz proporcionalmente. Card de detalhe (specs/medidas) tem no máximo **450px**, alinhado à esquerda (antes esticava por FILL até a largura toda do bloco) | Decisão de produto 2026-09-17, mesma leva da largura fixa acima |
| **"Nova Versão" (major) preserva a Ficha anterior e nasce ao lado dela; "Atualização" (minor) continua substituindo no mesmo lugar** — critério é o tipo já escolhido no modal "Versionar Ficha de Projeto" (`versioning-modal`), propagado como `versionType` no `create-handoff` | Decisão 2026-09-17: antes, TODA geração com ficha já existente removia a anterior, mesmo quando o designer escolhia "Nova Versão" (redesenho/mudança estrutural) — perdendo o histórico visual no canvas. Agora só "Atualização" (minor) remove; "Nova Versão" preserva a antiga como histórico e a nova nasce colada à direita dela. Ambas usam `_fichaBasePosition` como referência de Y (nunca a âncora do frame — ver decisão "Posição da Ficha nunca mais é adivinhada" acima) |
| Bloco "Exportar" da tela de Resumo **virou modal** (`export-modal`, `modals.html`), disparado por um botão secundário/outline logo após "Gerar Ficha" — antes era um accordion aberto por padrão no mesmo lugar | Decisão 2026-09-18: usuário achou o accordion "meio perdido", competindo com "Gerar Ficha" (a ação primária da tela) sem hierarquia visual clara. Todo o conteúdo (hint dispensável em bullets + checklist de 5 itens + botão "Exportar selecionados") foi movido pro modal sem alteração de comportamento; `toggleExportAccordion()` (`core.js`) removida por ficar órfã; `executeSelectedExports()` agora fecha o modal ao terminar a fila de exportação |

---

## Bugs corrigidos relevantes (contexto histórico)

- **Card de "Documentação Visual" sem contraste visual (v6.29.1, 2026-09-18)** — cada bloco de frame (`_hdBuildFrameShowcaseBlock`, `code.js`) tinha fill branco puro sobre o fundo branco da própria Ficha, com só uma borda de 1px separando um card do próximo — dificultava enxergar onde um card terminava e o outro começava, principalmente rolando a lista de vários frames documentados. Corrigido usando o mesmo tom cinza claro já padrão em outros cards da Ficha (`{ r: 0.98, g: 0.98, b: 0.99 }`, o mesmo de linhas de medida/regra/exceção).
- **Roxo/indigo de marca nova (ainda não vigente) vazado em `styles/plugin.css` (v6.29.4, 2026-09-18)** — `#btn-top:hover`, `.fab-inline` (base/hover/dark) e `.fab-main:hover` usavam `rgba(61,61,255,...)`, um roxo que não corresponde à cor de marca vigente da CAIXA (`#005ca9`, azul). Esse roxo não vinha do Tailwind (onde o token de marca já está correto) — estava hardcoded em CSS solto, por isso passou despercebido nas revisões anteriores de normalização de botões (ver seção "Normalização de botões contra o Design System"). Corrigido substituindo as 6 ocorrências por `rgba(0,92,169,...)` (RGB de `#005ca9`). **Padrão a vigiar:** cor de marca em `plugin.css` (fora do Tailwind) não é coberta pelas auditorias que buscam só classes/hex Tailwind — checar `rgba(...)` hardcoded também.
- **Card de detalhe (specs/medidas) nascia espremido (35px) em vez de 450px (v6.23.2, 2026-09-18)** — a implementação inicial da largura fixa de 450px (v6.23.0) usou `layoutSizingHorizontal = "HUG"` + `maxWidth = 450` no card de detalhe, mas `maxWidth` é só um TETO, nunca um piso — sem conteúdo largo o suficiente pra "puxar" o HUG, o card encolhia livremente até ~35px, reproduzido em teste real do usuário. Corrigido usando `resize(450, altura) + counterAxisSizingMode = "FIXED"` (dentro do pai VERTICAL "pair", largura é o eixo cruzado) — força os 450px de verdade, com o conteúdo interno (que já usa FILL) preenchendo essa largura, em vez de depender do conteúdo pra definir o tamanho do próprio container.
- **"Copiar contexto pro Figma Make" falhava com `navigator.clipboard.writeText` (v6.23.1, 2026-09-18)** — dentro do iframe sandboxed da UI do plugin Figma, a Clipboard API moderna nem sempre tem a Permissions Policy (`clipboard-write`) concedida, mesmo existindo como objeto — reproduzido em teste real do usuário ("Não foi possível copiar automaticamente"). Diferente do outro uso já existente de `navigator.clipboard.writeText` no projeto (`copyPendingComments`, `handoff.js`), que roda numa página HTML separada exportada pro navegador (fora do iframe do plugin, sem essa restrição) — os dois contextos não são equivalentes, copiar o padrão dali não bastava. Corrigido com fallback: se a Clipboard API não existir ou falhar, tenta `document.execCommand('copy')` via um `<textarea>` temporário fora da tela (método mais antigo, mas mais confiável em iframes restritos) antes de reportar erro de verdade ao usuário.
- **Chip de tipo de Cenário de Exceção sempre vermelho na Ficha, independente do tipo real (v6.22.2, 2026-09-17)** — a seção "Cenários de Exceção" (`code.js`) usava `{ r: 0.9, g: 0.2, b: 0.2 }` (vermelho) fixo pro `typeTag` de todo cenário, ignorando `e.tipo` — "Sucesso", "Alerta" e "Confirmação" apareciam vermelhos, sem nenhuma distinção visual entre os 4 tipos (Erro/Alerta/Sucesso/Confirmação). Corrigido usando a mesma paleta semântica já estabelecida na UI do plugin (`EXCEPTION_TYPE_COLORS`/`NEW_EXC_TYPE_BORDER`, `specifications.js`): Erro=vermelho, Alerta=âmbar, Sucesso=verde, Confirmação=azul (`#005ca9`).
- **Snapshot de "Documentação Visual" ilegível em frames largos (v6.22.2, 2026-09-17)** — o teto de largura do snapshot (specs/medidas marcadas sobre o frame real) era fixo em 680px — um frame como um header full-width (1440px real) ficava espremido a ponto do snapshot de medidas praticamente não dar pra enxergar. Aumentado o teto pra 1200px. Estrutura do bloco (título do frame → par snapshot+card de specs → par snapshot+card de medidas, empilhados, cada par já horizontal) confirmada como correta pelo usuário — não mudou, só a largura.
- **Token de cor/raio/tipografia/effect nunca era encontrado em vários pontos do scan — auditoria calculava conformidade a partir de valor bruto, não do token real (v6.22.0–v6.22.1, 2026-09-17)** — investigação teve 2 rodadas: a primeira (reportada pelo usuário com um caso real, card "NavBar" mostrando `#FFFFFF` em vez do token) achou 3 pontos com `node.boundVariables["fills"]`/`["strokes"]`; a segunda rodada (usuário pediu reconferir ícones/padding/gap, e depois tipografia/effects) achou mais 2 bugs do mesmo padrão estrutural e 2 lacunas nunca implementadas. Resumo completo:
  - **Bugs reais corrigidos** (caminho errado — o token existia no Figma, só nunca era encontrado): fill color e stroke color em 4 pontos (`extractNodeProperties` — a função central do **scan de auditoria DSC**, usada em TODO frame escaneado; `request-spec-properties` — card de detalhe de spec; detalhe "Appearance" de medidas customizadas) usavam `node.boundVariables["fills"]`/`["strokes"]`, mas o binding de cor de um paint vive dentro do próprio objeto paint, em `paint.boundVariables.color.id` (cada paint no array pode ter sua própria variável) — nunca em `node.boundVariables`. Além disso, **`cornerRadius`** (2 pontos: scan de auditoria e `request-spec-properties`) usava `getVar(n, "cornerRadius")`, mas esse campo **não é vinculável de verdade** na Plugin API — só os 4 cantos individuais são (`topLeftRadius`/`topRightRadius`/`bottomLeftRadius`/`bottomRightRadius`); corrigido para ler `topLeftRadius` como representante (só chega nesse código quando os 4 cantos já são iguais).
  - **Lacunas implementadas** (nunca tinha existido, não é regressão): tipografia por **variável** (fontSize vinculado a uma variable, mecanismo Figma separado de Text Style — o scan só cobria Text Style, ficando cego pra fontSize tokenizado sem estilo aplicado) e effects por **variável** (radius/spread/offset/color de shadow/blur vinculados a uma variable — binding vive dentro do próprio objeto effect, `effect.boundVariables.{campo}.id`, mesmo padrão estrutural de paint; o scan só cobria Effect Style).
  - Corrigido com helpers dedicados: `getPaintVar`/`getPaintVariableInfo` (lê `paint.boundVariables.color.id`) e `getEffectVar` (lê `effect.boundVariables[campo].id`), duplicados por escopo de função onde necessário. **Confirmado que padding/gap/itemSpacing/strokeWeight/fontSize (em `request-spec-properties`) já estavam corretos** — são campos escalares reais em `VariableBindableNodeField`/`VariableBindableTextField`, o padrão `node.boundVariables[prop]` sempre foi certo pra eles.
  - **Impacto retroativo:** frames já escaneados ANTES desta correção têm conformidade de cor/raio/tipografia/effect calculada a partir de valor bruto, não do token real — recomendação: re-escanear frames existentes (botão de refresh do scan) para recalcular corretamente.
  - **Padrão a vigiar:** qualquer nova extração de `boundVariables` precisa primeiro checar, na documentação oficial (`VariableBindableNodeField`/`VariableBindableTextField`/`VariableBindableEffectField`), se o campo é escalar (`node.boundVariables[prop]`) ou aninhado num array de sub-objetos com binding próprio (paint/effect — `item.boundVariables[campo].id`). Nunca assumir por analogia com outro campo já correto.
- **Fantasma de posição de spec sugerido perto do elemento, não da última spec (v6.21.1, 2026-09-17)** — `create-position-ghost` (`code.js`) sempre ancorava a sugestão de posição do fantasma no elemento sendo anotado (`targetNodeId`), espalhando as specs pelo canvas conforme a posição de cada elemento anotado, em vez de mantê-las organizadas em sequência. Corrigido: a sugestão agora prioriza a **última spec já criada** no projeto (`createdSpecs[createdSpecs.length - 1].id`, enviado pelo frontend como `lastSpecId` em `markSpecPosition()`) — o fantasma nasce ao lado dela; só cai pro elemento sendo anotado se ainda não existir nenhuma spec. Continua sendo só sugestão inicial: o designer arrasta o fantasma livremente pra qualquer lugar antes de confirmar, exatamente como já funcionava.
- **Cards de frame nasciam alguns abertos, outros fechados (v6.20.7, 2026-09-17)** — `renderFrameCard(frame, autoExpand)` tinha `autoExpand = false` por padrão; o card só nascia expandido quando registrado individualmente (`addFrame`, `core.js:728`, chamando `renderFrameCard(frame, true)`), mas ao recarregar a lista inteira (reabrir o plugin/tela) cada frame era renderizado sem esse parâmetro (`core.js:2639`), nascendo recolhido — daí a inconsistência visual. Corrigido separando as duas semânticas que estavam fundidas num único parâmetro: `autoExpand` (agora `true` por padrão, sempre nasce visualmente expandido, controla só a classe `hidden` do corpo + estado do chevron/aria via HTML) e `forceFocus` (novo parâmetro, `false` por padrão, controla só o `focusNode()` — que faz o viewport pular até o frame no canvas). Antes os dois efeitos rodavam juntos via `toggleFrameAccordion(fid)` no final da função; isso é exatamente o motivo pelo qual `autoExpand` tinha sido restrito ao registro individual num primeiro momento (comentário histórico no código: "abrir a tela 'jogava' o viewport pra cada frame do array sem nenhum clique do usuário") — separar expandir de focar resolve o pedido de UX sem reintroduzir aquele bug. `forceFocus: true` continua exclusivo do registro de um frame novo (`addFrame`).
- **Roxo/azul vibrante (`#3D3DFF`) da "nova marca" espalhado por toda a Ficha — correção mais completa que a da v6.16.0 (v6.20.6, 2026-09-17)** — a correção anterior (v6.16.0) só tratou o fundo externo da Ficha (`mainContainer`, um caso isolado com hex hardcoded diferente, `#00325b`). Ficou faltando o problema maior: `{ r: 0.24, g: 0.24, b: 1 }` (equivalente a `#3D3DFF`) estava hardcoded em **13 lugares diferentes** do gerador da Ficha (`code.js`) — títulos de seção ("Informações Básicas", "Equipe e Responsáveis" etc.), links ("Acesse o link", "Contato"), badges de papel da equipe, nome de item do relatório de auditoria, título "Relatório de Auditoria". Visualmente aparecia como um roxo/azul vibrante nítido, bem diferente do azul CAIXA institucional. Corrigido substituindo as 13 ocorrências por `hexToRgb("#005ca9")` (mesmo azul já usado no logo da CAIXA no header da própria Ficha, e nos botões/header da UI do plugin). **Padrão a vigiar:** antes de considerar uma correção de cor "completa", buscar por TODAS as ocorrências do valor RGB/hex errado no arquivo inteiro (`grep`), não só no elemento que apareceu no print — a mesma cor errada frequentemente está copiada em múltiplos pontos do gerador da Ficha, não centralizada num token.
- **"Registrar Frame" renomeado para "Escanear Frame" (v6.20.5, 2026-09-17)** — o rótulo do botão de ação em "Escanear Tokens" (header + empty-state, `handoff.html`) dizia "Registrar Frame", que soa como um cadastro genérico sem comunicar o propósito real da tela: escanear os tokens do frame contra o DSC pra auditoria. "Escanear Frame" alinha com o nome da própria tela ("Escanear Tokens") e com a ação que de fato acontece ao clicar. Também renomeado: empty-state "Nenhum frame documentado" → "Nenhum frame escaneado" (mesma inconsistência — "documentado" não é o verbo usado em nenhum outro lugar do fluxo de Tokens), e os textos correspondentes no onboarding/guide (`onboarding.js`, `guide.html`, duplicados nos dois lugares). Nome de função interna (`requestFrameRegistration`) e id do botão (`btn-frame-register-header`) mantidos — são internos, mudar sem necessidade seria risco à toa.
- **`_fichaBasePosition` sobrevivia à remoção da Ficha, pulando o modal de confirmação (v6.20.4, 2026-09-17)** — mesma classe de bug do `_fichaGenerated` (ver entrada abaixo), num campo diferente: `data._fichaBasePosition` (a coordenada salva na 1ª confirmação de posição) era usada sem nenhuma verificação de que a Ficha física ainda existia no canvas. Testada a funcionalidade várias vezes na mesma sessão antes desta correção — a Ficha real tinha sido apagada do canvas em algum teste, mas a coordenada continuava salva em `handoffData`, então a próxima geração pulava direto pro `handoff-complete` (usando a posição salva) em vez de `handoff-needs-position-confirmation`. Diferente de `_fichaGenerated`, essa coordenada não referencia nenhum nó específico pra "verificar contra o canvas" sozinha — corrigido vinculando sua validade à mesma fonte de verdade já resolvida (`_existingFicha`, resultado de `_hdFindExistingFicha` checado no início de `create-handoff`): a posição salva só é usada `if (_existingFicha && data._fichaBasePosition)`, senão cai no fluxo de primeira geração (posição sugerida + modal de confirmação) mesmo já tendo uma coordenada salva de antes.
- **`_fichaGenerated` ficava "preso" em `true` mesmo sem Ficha no canvas (v6.20.3, 2026-09-17)** — `createHandoffOnCanvas()` (`handoff.js`) confiava direto em `handoffData._fichaGenerated` (persistido em localStorage) pra decidir entre abrir o modal de versionamento ou gerar a Ficha do zero — nunca checava o canvas real. Uma vez `true` (setado ao confirmar a posição da Ficha pela primeira vez, ver `_markFichaGenerated`), o flag nunca mais voltava a `false` mesmo que a Ficha fosse apagada manualmente do canvas depois (ex: durante testes/limpeza do projeto) — resultado: "Gerar Ficha" perguntava sobre versionamento (Nova Versão/Atualização) sem nenhuma Ficha existir de verdade. Causa secundária no mesmo bug: o handler de `ficha-version-pulled` (`messages.js`) só atualizava `_fichaGenerated` para `true` quando encontrava uma ficha (`msg.versao` truthy) — nunca corrigia de volta pra `false` quando `msg.temFicha` era `false`. Corrigido nos dois pontos: `ficha-version-pulled` agora sempre sincroniza `_fichaGenerated = !!msg.temFicha` (nos dois sentidos); `createHandoffOnCanvas()` passou a disparar `pull-ficha-version-from-canvas` (mesmo handler já usado por `openHandoffInjectModal`) e só decidir entre versionamento/geração nova depois da resposta real do backend, nunca mais confiando cegamente no flag local. **Padrão a vigiar:** qualquer flag boolean persistido em `handoffData` que representa "algo existe no canvas" precisa de um caminho de correção nos DOIS sentidos (existe → true, não existe → false) — só cobrir a criação e nunca a remoção é a mesma classe de bug.
- **Card de hint repetia o texto dinâmico logo acima (v6.19.1, 2026-09-17)** — o modal "Gerar Ficha de Handoff" (`inject-state-single`, `modals.html`) tinha um card azul fixo ("Você pode continuar documentando para enriquecer a ficha antes de gerar.") logo abaixo do texto dinâmico `#inject-single-msg`, que já pergunta a mesma coisa ("Deseja gerar a ficha com o que tem, ou continuar documentando?") — os dois botões logo abaixo já tornam a decisão acionável. Removido o card por ser puramente redundante, mesmo padrão dos hints fixos já removidos em Specs/Medidas/Fluxos/Tokens (ver entrada "Cards de hint fixo" abaixo).
- **`[HighlightStroke]` órfão sobrando nas layers (v6.20.2, 2026-09-17)** — `focusNode()` (usado no clique de expandir/focar em Frames/Specs/Medidas/Fluxos) criava um retângulo `[HighlightStroke]` no canvas, referenciado só por uma variável em memória do processo do plugin (`activeHighlightNode`, `code.js`). Se o plugin recarregasse ou fechasse de forma atípica (crash, dev reload) enquanto um highlight estava ativo, a referência se perdia e o retângulo ficava esquecido pra sempre — nenhum mecanismo existente (`figma.on('close')`/`'currentpagechange'`, ambos corretos mas dependentes da mesma variável) cobria esse caso. Corrigido em duas frentes: (1) `focusNode()` parou de criar o stroke — passa a chamar `highlight-node` com `highlight: false`, só seleciona+rola até o elemento; a própria seleção nativa do Figma (contorno azul) já é destaque visual suficiente, sem precisar desenhar nada extra no canvas. O stroke fica reservado só pro preview de hover (`sendHighlight`/`clearHighlight`), que é sempre pareado no mesmo ciclo de `mouseenter`/`mouseleave` (risco residual bem menor). (2) Varredura de segurança no boot do plugin (`code.js`, logo após `figma.showUI`) remove qualquer `[HighlightStroke]` órfão que já existia na página de sessões anteriores a essa correção.
- **Toda a heurística de posicionamento automático da Ficha foi removida (v6.20.0, 2026-09-17)** — depois de 3 rodadas de correção na mesma sessão (ancorar no frame por `figmaId`; trocar direção de "à direita" pra "abaixo"; corrigir bug de prioridade que ignorava a âncora quando já existia ficha antiga) o usuário continuou reportando a Ficha nascendo "a milhões de pixels de distância" em teste real, mesmo sendo a primeira ficha do arquivo (sem ficha anterior "contaminando" nada). Causa raiz nunca totalmente isolada — mas o padrão real (heurística de âncora/colisão tentando adivinhar "o lugar certo") se provou não confiável o suficiente. Abandonada por completo em favor de confirmação manual explícita (ver decisão de produto "Posição da Ficha nunca mais é adivinhada"), o mesmo princípio já usado com sucesso pro posicionamento de specs (fantasma arrastável + "Usar esta posição"). Todo o código de ~150 linhas de prioridades/colisão foi deletado de `code.js` (`create-handoff`), substituído por ~30 linhas: posição sugerida simples na primeira vez, modal de confirmação, e reuso puro da posição salva depois disso.
- **Frame de "Novo Componente" na Ficha não trazia nada de construção (v6.18.1, 2026-09-17)** — o card de frame em "Frames Escaneados" mostrava só nome + badge "Novo componente" + auditoria, mesmo para frames marcados como novo componente pro DSC — faltava exatamente o que o dev precisa pra construir: a descrição do padrão de uso/nomenclatura/diretrizes (`frame.newComponentObservations`, campo de texto livre já existente na tela Escanear Tokens, só nunca tinha chegado à Ficha) e a lista de elementos que compõem o componente (todo o scan do frame — componentes/ícones/tipografia/vetores/frames, sem filtro de `isMarkedCustom`, já que aqui o objetivo é documentar o componente novo por inteiro). Corrigido em `_hdBuildFrameCard` (`code.js`): quando `f.isNewComponent`, adiciona os dois blocos ao card.
- **Fundo da Ficha ainda usava cor da "nova marca" não revertida (v6.16.0, 2026-09-17)** — `mainContainer` (o frame externo que envolve toda a Ficha gerada no canvas, `code.js:2106`) usava `#00325b`, cor que não aparecia em mais nenhum outro lugar do código — todo o resto do sistema (header do plugin, botões primary, logo da CAIXA no próprio header da Ficha) usa `#005ca9`/`#004d8d`, as cores documentadas pós-reversão de marca (ver "pré-reversão de marca" em Normalização de Botões, v6.11.0). Resquício que escapou daquela reversão por viver isolado dentro do gerador da Ficha, não nos botões da UI do plugin. Corrigido para `#004d8d`.
- **Sub-card de spec vinculada a frame (Escanear Tokens) tinha foco redundante (v6.15.7, 2026-09-17)** — dentro do sub-accordion "Tokens Escaneados" de um frame, cada spec vinculada tinha um botão `locate` (foco) ao lado do olho de visibilidade no header, mas `toggleSpecDetails` (diferente de `toggleAccordion`/`toggleFrameAccordion`) não focava ao expandir — então o botão não era redundante por acidente, era a ÚNICA forma de focar. Corrigido junto: `toggleSpecDetails` passou a receber `nodeId` e focar ao expandir (mesmo princípio dos outros accordions), e só então o botão `locate` foi removido do header. **Padrão a vigiar:** antes de remover um ícone de foco por "parecer redundante com expandir", confirmar que a função de toggle daquele accordion especificamente já chama `focusNode`/`highlight-node` — nem todo toggle do plugin faz isso (varredura 2026-09-17 confirmou que cards de item "folha" sem expandir, como o card de token escaneado e o item de Fluxos de Tela, não têm esse problema: o card inteiro já é o único gatilho de clique, sem concorrência com nenhum "expandir").
- **Ícone de foco dedicado removido do card de frame — redundante com expandir (v6.15.6, 2026-09-17)** — o botão `locate` no header de `renderFrameCard` (Escanear Tokens) fazia exatamente o que `toggleFrameAccordion` já faz ao expandir (`focusNode(frame.figmaId)`, `core.js:807`). Removido por ser 100% redundante — clicar para expandir já foca/rola até o elemento no canvas. **Princípio adotado (vale para qualquer accordion de item no Handex):** ação de **foco** (navegacional, "me leve até lá") não precisa de ícone próprio — expandir já cobre; só ações com estado próprio e persistente no objeto (ex: visibilidade/olho, que liga/desliga sem exigir expandir) continuam como ícone dedicado no header. Não confundir as duas categorias ao revisar outros accordions (Fluxos, Briefing — ver `handex_padrao_accordion_pendente` em memória).
- **Preview de highlight no hover do card de medida removido (v6.15.5, 2026-09-17)** — `renderMeasurementsResults` (`measurement.js`) tinha um listener `mouseenter`/`mouseleave` no header do card que disparava/limpava um highlight transitório no canvas (`sendHighlight`/`clearHighlight`) independente do estado de expandido. Removido a pedido do usuário: o foco no canvas agora só acontece ao expandir o card (`toggleAccordion` já chama `highlight-node` com `shouldScroll: true`), sem nenhum efeito de hover — consistente com Specs, que nunca teve esse comportamento de hover.
- **Barra "Ações" separada nos accordions de item — padrão inconsistente (v6.15.3–v6.15.4, 2026-09-17)** — o card de medida (Anotar Medidas), o card de spec (Anotar Specs) e o card de frame (Escanear Tokens, `renderFrameCard`) tinham o header de expandir/focar separado de uma segunda linha rotulada "Ações" (foco/olho + lixeira), ocupando espaço extra e divergindo entre telas. Corrigido nos 3: ação de foco/visibilidade subiu para a mesma linha do chevron de expandir (sem rótulo "Ações"); excluir virou **botão grande** dentro do conteúdo expandido (`Remover Medida`/`Excluir Especificação`/`Remover Frame`), nunca mais ícone solto no header nem numa barra própria. **Decisão de produto (não reverter sem alinhamento):** todo accordion de item no Handex deve seguir esse esqueleto — header com ações rápidas específicas (foco, visibilidade, menu "...") + chevron; excluir sempre como botão grande no rodapé do conteúdo expandido. Ainda pendente de padronizar (mapeado, não tratado nesta entrega): Fluxos de Tela (conexões/jornadas), Briefing.
- **Cards de hint fixo duplicavam o onboarding/empty-state (v6.15.2 e v6.16.1, 2026-09-17)** — as 4 telas (Escanear Tokens, Anotar Specs, Anotar Medidas, Fluxos de Tela) tinham cada uma um card azul fixo ("Selecione X no canvas e toque em/no botão + ...") sempre visível, repetindo a mesma explicação já coberta pelo empty-state e pelo onboarding contextual (ícone de graduation-cap). Removidos os 4 cards (`frames-register-hint`, `hint-specs`, `hint-measures`, `hint-flows`) e as chamadas órfãs a `_updateContentHint` que os controlavam — dúvida de "como funciona" agora só é resolvida pelo empty-state/onboarding, não duplicada em UI fixa. `frames-register-hint` (Escanear Tokens) só foi corrigido na v6.16.1, uma rodada depois dos outros 3 — **padrão a vigiar:** ao receber "remove esse card repetitivo" apontando UM print, procurar por todo o padrão nas 4 telas equivalentes antes de considerar a tarefa fechada, não só na tela do print. Aproveitado (v6.15.2) para corrigir o texto do empty-state de Fluxos ("Selecione 2 elementos" → "Selecione 2 ou mais elementos"), que estava incorreto (o plugin aceita e conecta em sequência 3+ frames).
- **Tooltip do ícone de ajuda do Frame Selector não aparecia (v6.15.2)** — atributo `title` direto num `<svg>` (gerado pelo Lucide a partir de `<i data-lucide="help-circle">`) não dispara o tooltip nativo do navegador de forma confiável. Corrigido envolvendo o ícone num `<span title="...">` com o `<svg>` interno marcado `pointer-events-none` — padrão a repetir em qualquer ícone Lucide que precise de tooltip nativo via `title`.
- **Contador "Tokens Escaneados" e botão "Continuar documentando" (v6.15.1)** — o contador do topo da tela de Resumo dizia "Tokens Escaneados (N)" mas `N` sempre foi contagem de **frames**, não de tokens individuais; corrigido para "Frames Escaneados". O botão "Continuar documentando" no modal de item único (`inject-state-single`, `modals.html`) só fechava a modal sem navegar; corrigido para também chamar `navigate('view-home')`.
- **`addFrame()` inicializava `audit` errado** — usava `{ status, justificativa }` mas o runtime espera `{ checkDone, semDesvios, observacoes, ressalvas }`. Corrigido em v4.1.4.
- **`setSvgColor` pintava o FRAME container do SVG** — `figma.createNodeFromSvg()` retorna um FRAME envolvendo VECTORs. A função aplicava cor no container, deixando tudo cinza. Corrigido para limpar fills do container e colorir só os nós folha.
- **`toggleFrameAccordion` buscava `frame-arrow-{id}`** — o HTML renderiza `frame-chevron-{id}`. Corrigido em v4.1.6.
- **Contador do topo de "Escanear Tokens" rotulado errado** — `_updateEmptyFramesState` (`core.js`) contava `handoffData.frames.length` (número de FRAMES registrados) mas exibia o rótulo "Tokens Escaneados (N)" — cada frame pode ter dezenas de tokens (componentes/ícones/tipografia), então o número nunca batia com "tokens". Corrigido em v6.15.1: rótulo passou a "Frames Escaneados (N)", mesma contagem.
- **"Continuar documentando" no modal "Gerar Ficha de Handoff" fechava a modal sem navegar** — no estado `inject-state-single` (`modals.html`), o botão só chamava `closeModal(...)`, diferente do estado `inject-state-empty` (mesma modal, mesmo texto de botão) que corretamente navegava pra `view-dados-projeto`. Resultado: usuário clicava "Continuar documentando" e a modal simplesmente sumia, sem voltar pra home nem indicar pra onde ir. Corrigido em v6.15.1: agora chama `navigate('view-home')`, igual ao padrão de fechar+navegar já usado no outro estado da mesma modal.
- **Card "Frame Selector" (Anotar Specs/Anotar Medidas) sempre expandido, ocupando espaço sem indicar claramente sua função** — v6.15.1: virou accordion recolhido por padrão (`toggleFrameSelectorCard`, mesmo padrão de `toggleFrameAccordion`), com ícone de ajuda fixo no cabeçalho (tooltip nativo `title`) explicando a função mesmo fechado. Cuidado ao mexer: quando há múltiplos frames mapeados e nenhum selecionado, o card precisa expandir automaticamente (`_updateFrameSelectorCopy`) e mostrar um ponto de alerta âmbar no cabeçalho — esse alerta antes vivia só na borda do `<select>`, que ficaria escondida dentro do accordion fechado sem esse ajuste.
- **`handoffData.docs` não inicializado no schema v2** — acessos como `handoffData.docs.proto.link` quebravam. Corrigido com null-guards.
- **Spec criada em "Anotar Specs" sumia da lista sem erro visível** — handler `spec-created` (`messages.js`) tinha `if (activeFrameId) { const frame = getFrame(activeFrameId); if (frame) { ...push... } }` sem `else` para o caso `frame === null`. `activeFrameId` é estado do módulo, nunca resetado por navegação — se ficasse "fantasma" apontando pra um frame excluído (ou de sessão anterior com outro arquivo `.fig` aberto), a spec não caía em nenhum array (nem `frame.createdSpecs`, nem `createdSpecs` global), mas `saveSpecsToStorage()` e o toast "Especificação criada" rodavam do mesmo jeito — o card era criado normalmente no canvas (responsabilidade do backend, independente do frontend), mascarando a falha por completo. Corrigido em 2026-08-27: adicionado `else` tratando `activeFrameId` truthy + frame inexistente como spec avulsa, e resetando `activeFrameId = null` pra não repetir o problema na spec seguinte. **Padrão a vigiar:** qualquer `if (algumEstadoDeModulo) { const x = lookup(...); if (x) { ... } }` sem `else` no nível de fora é um candidato a falha silenciosa — o `if` externo garante execução, mas o `if` interno pode abortar sem sinalizar nada ao usuário.
- **Sub-componentes estruturais auditados isoladamente (ex: "Icon color" dentro de `.[dsc] Menu Hamburger Header`)** — a proteção `_hasDSChild` (não auditar um container que tem filho DS real, porque a conformidade "vive" no filho) só cobria `category === "frames"`. Um COMPONENT/COMPONENT_SET usado como sub-composição interna de um componente maior (ex: wrapper de ícone) era auditado sozinho contra o skeleton via `componentKey`, que nunca bate porque sub-componentes internos não são publicados independentemente — resultado: item "FORA DO PADRÃO" mesmo com todas as props corretas. Corrigido em v6.8.2 estendendo `_hasDSChild` para `components`/`icons`, restrito a `node.type !== 'INSTANCE'` (instâncias reais podem legitimamente conter sub-instâncias e continuam auditadas normalmente).
- **Componente sem vínculo real de lib reportado como "EM CONFORMIDADE"** — `dsElement` (isDS no nível do componente) virava `true` sempre que a coleção agregada de props batia, mesmo quando o próprio componente não tinha `componentKey` reconhecido no skeleton, ou era instância de qualquer arquivo "remoto" do Figma (não necessariamente uma lib do DSC). Ex. real (2026-09-03): "NavBar" é instância de um componente publicado num arquivo pessoal de outro projeto de design (`mainComponent.remote === true`), mas não existe NavBar componentizado no DSC — o override `node.type === 'INSTANCE' && mainComp.remote` tratava esse vínculo genérico como prova de conformidade. Corrigido em duas rodadas:
  - **v6.8.2** (incompleta): tentou exigir vínculo real, mas ainda considerava `mainComponent.remote` como vínculo válido — não resolveu o caso NavBar porque "remoto" ≠ "é do DSC".
  - **v6.8.3** (definitiva): removido o override de `remote` genérico. Vínculo real com o DSC passou a ser só `matchedBy === "key"` (bateu no skeleton das libs DSC) ou convenção `[dsc]` no nome. Sem isso, o item vira `isDS: "warning"` + `isCustomComponent: true` (UI exibe "COMPONENTE PERSONALIZADO"). Além disso, componentes COM vínculo real passaram a herdar a mesma agregação de props já usada em frames — uma instância legítima do DSC com propriedades fora do padrão (gap/padding customizados) não é mais reportada como 100% conforme só por ter vínculo; a conformidade final é vínculo real **E** props OK.
  - **Padrão a vigiar:** `remote === true` no Figma significa "vem de algum arquivo publicado como biblioteca", nunca "é do Design System oficial" — qualquer decisão de conformidade contra o DSC precisa checar o `componentKey`/skeleton, nunca só a flag de remoto.
- **Medidas avulsas (sem frame vinculado) omitidas da contagem e da Ficha exportada** — `handoffData.measurements` (medidas criadas sem `activeFrameId`, ex: um grupo de medida solto na página) é disjunto de `frame.measurements` (mutuamente exclusivo, ver `messages.js` handler de `create-measurement`: `if (activeFrameId) { frame.measurements... } else { handoffData.measurements... }`). Cinco pontos diferentes do código somavam/exibiam só `frame.measurements`, ignorando as avulsas por completo: contador "Medidas" da tela de Resumo do Handoff (`hs-count-measures`, `core.js`), checklist de pré-requisitos do modal "Gerar Ficha" (`totalMeasures`, `handoff.js`), seção "## Medidas" do Markdown exportado (`handoff.js` — medidas avulsas simplesmente não apareciam no documento), accordion "Medidas" da Ficha HTML interativa (`handoff.js` — a seção inteira nem aparecia se só houvesse avulsas) e a seção "Medidas" da Ficha gerada no canvas do Figma (`code.js`, backend — mesmo problema, mais grave por ser o artefato principal entregue ao dev). Corrigido em v6.11.1, seguindo o mesmo padrão `__loose__`/"Sem frame vinculado" já usado (corretamente) para specs avulsas desde antes.
- **Wrapper estrutural auditado isoladamente mesmo com filho DS real — critério certo é vínculo, nunca nome** — a proteção original (`_hasDSChild`, v6.8.2) só pulava containers estruturais quando `node.type !== 'INSTANCE'`, partindo da premissa "instância real do DSC pode legitimamente conter sub-instâncias e ainda ser o item correto a auditar". Isso vale para uma instância COM vínculo real, mas não cobre wrappers internos da própria lib que também são `INSTANCE` mas SEM vínculo próprio (ex: `.[base] Menu logo`, contendo `[dsc] Slot` como filho real). Uma primeira tentativa de correção (v6.8.4) tentou resolver isso detectando o prefixo de nome `.[base]` via regex — **rejeitada pelo usuário** por se basear em convenção de nomenclatura (texto livre, subjetivo, editável por qualquer designer), não em dado real de vínculo com a lib. Reescrita em v6.8.5 com o critério correto: calcula se o PRÓPRIO nó tem vínculo real com o DSC (`componentKey` batendo no skeleton via `matchedBy` "key", ou convenção `[dsc]` — nunca `.[base]` nem qualquer outro nome, nunca `mainComponent.remote`) *antes* de decidir se ele é estrutural; um nó sem vínculo próprio é pulado se tiver algum descendente (checado recursivamente, também por vínculo real de `componentKey`, não por tipo de nó cru) que tenha esse vínculo. `_ownLibLink`/`_nodeHasRealLibLink`/`_hasRealDSDescendant` substituíram `_hasDSChild`/`_isBaseWrapper`/`_isStructuralContainer` por completo. **Padrão a vigiar:** nome de camada no Figma nunca é dado confiável para decisão de conformidade — é texto livre. Toda decisão de "isso é do DSC" precisa passar por `componentKey` contra o skeleton (ou, no máximo, pela convenção `[dsc]` já estabelecida e documentada como tal, nunca uma convenção nova inferida ad-hoc).

**Nota sobre organização do canvas por Sections — v6.11.1 (2026-09-11):** todo conteúdo criado pelo Handex no canvas (medidas, specs, fluxos, a Ficha de handoff) era `figma.currentPage.appendChild()`'d diretamente — solto na página, identificado só por `handexCategory` (pluginData invisível na árvore de camadas normal do Figma). Achado real (usuário revisando a árvore de camadas): sem nenhuma organização visível, tudo misturado. Corrigido criando 4 `SECTION`s reaproveitáveis por categoria (`Handex | Medidas`, `Handex | Specs`, `Handex | Fluxos`, `Handex | Ficha`), geridas por `_hdEnsureCategorySection()`/`_hdMoveToCategorySection()` (`code.js`, topo do arquivo).

`SECTION` foi escolhida em vez de `GROUP`/`FRAME` **depois de teste manual confirmado no arquivo real do usuário**: mover um nó pra dentro de uma Section não recalcula sua posição x/y (diferente de `group()`/`frame.appendChild()`, que sempre tornam a posição relativa ao novo pai) — essencial porque medidas e conectores de fluxo precisam continuar ancorados exatamente no elemento real do design que anotam, nunca reposicionados por entrar num container. Não confundir com a decisão de "GROUP + nó solto" da spec logo abaixo — são camadas diferentes: aquela decisão é sobre o `specGroup` em si (marcador fora do grupo, dentro da mesma Section); esta é sobre onde os `specGroup`s (todos) ficam organizados na árvore.

**Efeito cascata que quase passou despercebido — múltiplos pontos do código assumiam que specs/medidas/fluxos/a Ficha eram filhos DIRETOS de `figma.currentPage`, e pararam de funcionar silenciosamente ao migrar pra dentro de Sections.** Corrigidos junto: `_reorderSpecGroupByTag()` (reordenava specs comparando índices em `figma.currentPage.children` — agora usa `specGroup.parent`, e o `insertChild` final também mudou de `figma.currentPage` pro container real, senão desfaria o `_hdMoveToCategorySection` da própria spec que acabou de mover); `_hdFindExistingFicha()` e a checagem de "ficha próxima" no posicionamento de uma nova ficha (ambas só olhavam `figma.currentPage.children`, agora olham também dentro da Section "Handex | Ficha"); `delete-canvas-content` (limpar tudo do canvas — a varredura só olhava filhos diretos da página, silenciosamente parava de encontrar qualquer coisa pra remover); empilhamento de specs pela mesma letra/tag (`_stackScanNodes`, cálculo de posição pra não sobrepor specs). **Padrão a vigiar:** qualquer novo código que assuma "conteúdo do Handex é filho direto da página" precisa "achatar" as Sections (`n.type === 'SECTION' ? n.children : [n]`) antes de operar — a lista de pontos corrigidos acima não é necessariamente exaustiva.

**Nota sobre a arquitetura de spec (GROUP + nó solto) — vigente:** o marcador ("Destaque") vive FORA do `specGroup`, como nó irmão travado na página, vinculado por pluginData (`handexSpecMarkerFor`/`handexSpecMarkerId`). Não é escolha estética, é a única solução dada uma limitação real da Figma Plugin API: a posição de um filho é sempre relativa ao pai (tanto em GROUP quanto em FRAME), e `locked = true` num filho não o desacopla de transformações do container pai — mover/redimensionar o container sempre move todos os filhos, travados ou não. Como o usuário sempre seleciona e arrasta o `specGroup` inteiro como unidade com um clique simples (nunca duplo-clique para "entrar" no container), qualquer filho do mesmo grupo se moveria junto, mesmo travado. Em 2026-08 essa arquitetura foi migrada para FRAME único (contour+dots dentro do mesmo container, exigindo duplo-clique do usuário para mover Conector/card sem arrastar o marcador) e depois **revertida** — o gesto de duplo-clique não funcionou bem na prática. Não remigrar sem alinhamento explícito.

---

## Convenções de código

- **Sem comentários** salvo quando o "porquê" é não óbvio
- **Sem tratamento de erro** para cenários impossíveis — só em boundaries externos
- Texto da UI sempre em **português brasileiro**
- Ícones: biblioteca **Lucide** (`data-lucide="nome"`)
- Estilo: **Tailwind v3** (classes compiladas via `bundle:ui`)
- Bordas dos botões: **`rounded-2xl`** em todo o plugin

---

## Normalização de botões contra o Design System (v6.11.0)

Auditoria ampla em todo o plugin encontrou inconsistência real: botões que cumprem a mesma função (avançar/confirmar num modal, cancelar/fechar) usavam cores diferentes em telas diferentes, mesmo com o catálogo de 6 variantes já documentado em `docs/site/design-system.html`.

**Achado principal — onboarding:** o botão de avançar/concluir (`onboarding.js`, funções `_renderOnboardingModal`) usava `style="background-color:${tool.color}"` — uma cor **por ferramenta** (`ONBOARDING_TOOLS`, ex: `#4f46e5` indigo em Specs, `#9333ea` purple em Fluxos), o mesmo sistema de identidade visual usado no ícone/barra de progresso/destaque de propósito do modal. Isso é intencional para os elementos decorativos, mas fazia o único botão de ação do modal mudar de cor sem nenhum significado semântico (indigo/purple não representam nada em nenhum outro lugar do plugin). **Corrigido:** o botão de ação agora é sempre azul de marca (`bg-blue-500 hover:bg-blue-600`) e "Pular"/"Voltar" sempre outline (`bg-white border border-gray-200 ...`) — em todas as ferramentas, independente de `tool.color`. A cor por ferramenta permanece intocada nos elementos decorativos.

**Achado secundário — ~15 botões primários com hex solto:** vários modais (`modals.html`) e views (`guide.html`, `home.html`, `dados-projeto.html`, `handoff-summary.html`) usavam `bg-[#005ca9]`/`bg-[#004d8d]` direto em vez da classe nomeada `bg-blue-500`/`bg-blue-600` — mesma cor visual (confirmado: `blue-500` no `tailwind.config.cjs` já é `#005ca9`), mas sem o token. Inclui 4 ocorrências de hex **pré-reversão de marca** (`hover:bg-[#004d8f]`/`hover:bg-[#005a8e]`) já listadas como dívida técnica antiga — essas foram corrigidas junto.

**Achado terciário — botões "Cancelar/Voltar/Fechar" sem padrão único:** a maioria usava `bg-gray-100 hover:bg-gray-200` (cinza sólido) ou texto puro sem fundo/borda — nenhum dos dois é a variante `outline` documentada (branco + borda cinza fina). Normalizados para `bg-white dark:bg-dark-surface border border-gray-200 dark:border-dark-line hover:bg-gray-50`.

**Nota importante sobre as classes `.hx-btn-*`:** essas classes **não existem como CSS real no plugin** — só existem dentro do `<style>` de `docs/site/design-system.html`, que é documentação, um arquivo HTML separado com seu próprio stylesheet. O código real do plugin usa Tailwind puro em cada botão (`bg-blue-500 hover:bg-blue-600 text-white rounded-2xl font-bold ...`). O catálogo documenta o padrão visual esperado, não uma API de classes reutilizável — ao aplicar/verificar o padrão em código, sempre usar Tailwind direto, nunca escrever `class="hx-btn hx-btn-primary"` esperando que funcione.

**Não mexido (confirmado intencional):** botão "Usar esta posição" (âmbar, `modals.html`) — documentado por comentário explícito no próprio código como estado semântico de "ação em andamento no canvas", distinto do fluxo padrão. Botão "Conectar Agora" em estado disabled com cor de fundo própria (`bg-gray-300`) em vez de opacity sobre a cor normal — é uma correção de lógica condicional mais delicada, deixada de fora desta rodada.

---

## Design System do Handex (leitura obrigatória antes de mudança visual)

`docs/design-system-handex.md` é a **referência normativa** de tokens (cor, tipografia, espaçamento, radius) e catálogo de componentes (variantes de botão, accordion, modal, ícone) da UI do plugin — não confundir com o DSC da CAIXA, que é outro documento (ver seção "Arquitetura" acima, `refs/_manifest.json`).

**Antes de criar ou alterar qualquer componente visual** (botão, card, modal, ícone, cor, espaçamento): consultar esse arquivo primeiro. Se o padrão já existe lá, seguir — não inventar uma variante nova sem necessidade. Se a mudança que você está fazendo torna uma regra do documento desatualizada (novo token, variante nova, correção de contraste, etc.), **atualizar o `.md` no mesmo commit** — nunca deixar código e documento divergirem silenciosamente, é exatamente esse tipo de duplicidade sem sincronização que já causou bugs reais neste projeto (ver "Bugs corrigidos relevantes").

Onde o código atual diverge do que o documento define, isso é dívida técnica listada na seção 9 do próprio `.md` — não uma segunda opção válida a copiar.

Há também uma versão navegável publicada como Artifact (link não fixo, buscar via `Artifact action:"list"` se precisar) — é gerada manualmente a partir do `.md`, não é fonte, só vitrine. Se o `.md` mudar, o artifact deveria ser republicado para não ficar defasado.

---

## Estrutura da home (referência visual)

**Corrigido em 2026-08-24** — a versão anterior deste bloco descrevia um header com "Gerar Ficha" e busca que não existe mais no código (`src/plugin/ui.html:616-667`); ficou desatualizada silenciosamente por pelo menos uma versão e induziu análise incorreta numa auditoria de UX. Verificar contra `ui.html` antes de confiar neste bloco em revisões futuras.

```
Header: [Logo | HANDEX vX]  [📋 Dados do Projeto]  [🔍− zoom out (oculto por padrão)]  [🔍+ zoom in]  [☀/🌙 tema]  [⇱ minimizar]

Grid 2×3 (flex-1, preenche altura disponível):
  [Como usar o plugin]   [Informações do Projeto]
  [Escanear Tokens]      [Anotar Specs]
  [Anotar Medidas]       [Fluxos de Tela]

Footer:
  [▶ Gerar Ficha de Handoff]
  [↑ Importar JSON]  [🗑]
```

"Gerar Ficha" não está mais no header global — hoje só é acionável de dentro de `view-dados-projeto` (`dados-projeto.html:314`) ou `view-handoff-summary` (`handoff-summary.html:146`), ambos chamando `openHandoffInjectModal()`.

**Botão de ação header ↔ empty-state (v6.10.0):** as 4 telas com um botão de ação principal no header (Escanear Tokens "Escanear Frame" — renomeado de "Registrar Frame" em v6.20.5, ver "Bugs corrigidos relevantes", Anotar Specs "Nova spec", Anotar Medidas "Inserir medida", Fluxos de Tela "Conectar Frames") escondem esse botão do header enquanto a lista está vazia — nesse estado, o mesmo botão (mesmo `onclick`, visual `fab-inline` idêntico) aparece como CTA centralizado dentro do empty-state. Assim que o primeiro item é criado, o botão do header reaparece e o empty-state (com seu botão) desaparece. O link de texto sublinhado que antes duplicava a ação dentro da frase instrutiva do empty-state foi removido nas 4 telas — a frase agora só descreve a ação sem repetir o botão.

Implementação: **dois botões reais** (um `id` no header + hidden por padrão, um centralizado no empty-state), nunca reparenting de nó via JS — decisão deliberada porque em 3 das 4 telas (Specs, Medidas, Fluxos) o empty-state é gerado via `innerHTML = template` a cada render (não é HTML fixo), então o botão central "nasce e morre" junto do template; reparenting exigiria capturar a referência do nó do header antes de sobrescrever o container, quebrando o padrão simples já usado no projeto. Toggle de visibilidade do botão do header amarrado à mesma condição `hasItems`/`hasFrames` já usada para os outros elementos da tela (seção título, botões de exportar/ocultar todos etc.):
- Tokens: `updateEmptyFramesState()` (`core.js`) — botão `#btn-frame-register-header`.
- Specs: `renderSpecsList()` (`specifications.js`) — botão `#specs-header-action-form` (id já existia, antes nunca era usado para toggle).
- Medidas: `renderMeasurementsResults()` (`measurement.js`), só no branch `!frameId` (tela standalone — o mesmo render também é usado por frame dentro do hub de Tokens, onde não há header próprio) — botão `#btn-measure-header`.
- Fluxos: `renderFlowsList()` (`specifications.js`) — botão `#btn-flow-connect-header`. Independente do segundo botão do header desta tela (`#btn-resync-flows`, ação secundária de redesenhar linhas — segue sua própria regra, só aparece com fluxos existentes, não faz parte desta troca).

**Ícones de ajuda migram de lado junto com o botão (mesmo dia):** quando o botão de ação sai do header, os ícones de ajuda que ficavam ao lado do título (graduation-cap de onboarding + o segundo ícone específico de cada tela, quando houver — `frame-status-help` em Tokens, `circle-help`/`openHelp` em Specs) migram para o grupo direito do header, preenchendo o espaço vazio; voltam para o lado do título assim que o botão de ação reaparece. Implementado com **reparenting real** (`appendChild`/`insertBefore`), diferente da técnica de "dois botões duplicados" usada pro CTA — aqui é seguro porque os ícones vivem em HTML fixo (não são regenerados via `innerHTML` a cada render). Função utilitária `_moveHeaderHelpIcons(helpIconsId, rightGroupSelector, hasContent)` (`core.js`), chamada nos mesmos 4 pontos que já fazem o toggle do botão de ação (`updateEmptyFramesState`, `renderSpecsList`, `renderMeasurementsResults`, `renderFlowsList`). O grupo esquerdo de destino é sempre resolvido via `header.querySelector(':scope > div:first-child')` a partir do header (nunca por posição relativa ao próprio wrapper de ícones, que muda de pai quando é movido).

**Bug real corrigido no mesmo dia — `.fab-inline` sobrescrevia `.hidden`:** o botão do header ficava visível mesmo com a classe `hidden` aplicada. Causa: no `<style>` compilado (`build.cjs`), `tailwind-compiled.css` (define `.hidden{display:none}`) é injetado ANTES de `plugin.css` (define `.fab-inline{display:inline-flex}`) — com especificidade igual (uma classe cada), a regra que vem depois no documento vence, então `.fab-inline` sempre ganhava de `.hidden` no mesmo elemento. Corrigido com uma regra específica `.fab-inline.hidden{display:none}` em `plugin.css`, logo após a definição base de `.fab-inline` — sem depender de reordenar os dois arquivos CSS (mudança de ordem teria efeito colateral maior, imprevisível sobre outras classes).

**Badge de check + label por card (v6.9.0):** os 5 cards da home que representam dado do projeto (`dados-projeto`, `tokens`, `specs`, `measurement`, `flows` — não `guide`, que é onboarding) exibem, no canto **superior esquerdo** (alinhado ao padding interno do botão, `top-3 left-3`), um badge verde circular com check quando já têm documentação salva — acompanhado de um texto curto fixo (`.home-card-check-text`) à direita do badge. O texto é sempre "Ação + particípio" e, quando o card tem uma contagem real, ela vai entre parênteses:
- `dados-projeto`: "Informações salvas" — **sem contagem** (critério booleano: `step1.titulo` preenchido **e** pelo menos 1 item em `step1.equipe`, não uma lista de itens).
- `tokens`: "Tokens escaneados (N)" — N = nº de frames com `specs` não vazio (qualquer categoria: components/icons/typography/vectors).
- `specs`: "Specs criadas (N)" — N = soma de `createdSpecs` por frame + `handoffData.specs` avulsas.
- `measurement`: "Medidas inseridas (N)" — N = soma de `measurements` por frame + `handoffData.measurements` avulsas.
- `flows`: "Fluxos mapeados (N)" — N = itens em `handoffData.createdFlows`.

`getHomeCardsDocumentedState()` (`design-data.js`) retorna `{ done, label }` por card — `label` já vem pronto para exibição, contagem incluída quando aplicável. Aplicado via `updateHomeCardsCheckState()` (`core.js`), chamada nos mesmos dois gatilhos de `updateHomeFooterButtonsState()` (entrar na home + `init-plugin`) — nunca recalculado em pontos isolados de criação de dado, para não arriscar ficar dessincronizado.

---

## Distribuição

A pasta `handex-plugin/` (gitignored) contém os 3 arquivos para distribuição:
- `manifest.json` + `code.bundle.js` + `ui.html`
- Instalação: Figma → Plugins → Development → Import plugin from manifest
- Não requer npm install nem build

---

## Observações de processo

- Documentação técnica: `BUSINESS_RULES.md` + `CHANGELOG.md` — atualizar a cada versão
- O `_skeleton.json` das libs DSC precisa ser atualizado periodicamente via `npm run refs:update` (requer `FIGMA_TOKEN` no `.env`)

### Versionamento (obrigatório a cada commit extenso)

- **Minor a cada entrega grande** (conjunto de features/fixes que muda comportamento perceptível do produto): `5.0.0 → 5.1.0 → 5.2.0`. Patch (`5.0.x`) fica reservado só para ajustes pequenos isolados.
- Bump em `package.json` (`npm version <x.y.z> --no-git-tag-version`) e em `CLAUDE.md` ("Versão atual") **antes** de commitar, não depois — nunca deixar a versão do código dessincronizada do relatório de atividades.
- Beta e estável versionam de forma independente: a beta usa sufixo `-beta.N` (ex: `5.1.0-beta.1`) enquanto está em teste; ao promover pra `main`, o sufixo é removido.
- `code.bundle.js` já lê a versão do `package.json` automaticamente via `scripts/bundle-code.cjs` — rodar `npm run bundle:code` depois do bump para propagar.

### Versão do `package.json` ≠ "Version N" da Figma Community

São **dois números completamente independentes** — não confundir nem tentar sincronizar um com o outro:

- **`package.json` (semver, ex: `6.1.1`)**: versionamento interno do código, segue as regras acima (minor/patch, beta com sufixo).
- **"Version N" da Figma Community** (ex: "Version 7", "Version 8"): contador próprio da Figma, incrementado automaticamente a cada publicação de nova versão no Community, visível em Community → Manage → Version history. Não é configurável, não lê `package.json`, e não tem relação matemática com o semver do projeto — a publicação da v6.1.1 pode virar "Version 8" na Community mesmo sem qualquer conexão numérica entre os dois.

Ao falar de "nova versão" com o time, sempre deixar claro qual dos dois números está em jogo — já causou confusão real (2026-07-28: tentativa de forçar o `package.json` para `8.0.0` para "alinhar" com o Version 7/8 da Community, revertida depois de esclarecido que são sistemas distintos).
