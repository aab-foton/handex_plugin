{
  "name": "ux-repo-caixa",
  "version": "1.0.2",
  "description": "Figma plugin for UX Repo Caixa",
  "scripts": {
    "dev": "npm run build:ui && npm run build:plugin -- --watch",
    "build:ui": "vite build --config vite.ui.config.ts",
    "build:plugin": "vite build --config vite.plugin.config.ts",
    "build": "npm run build:ui && npm run build:plugin"
  },
  "dependencies": {
    "jszip": "^3.10.1"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.88.0",
    "typescript": "^5.3.3",
    "vite": "^5.1.4",
    "vite-plugin-singlefile": "^2.0.1"
  }
}
